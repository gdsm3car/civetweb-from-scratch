# 里程碑 04：线程池

> **目标**：引入多线程，让服务器能同时处理多个客户端。

## 前置

完成里程碑 03

## 任务

1. 用 pthread 创建线程池（固定 N 个线程）
2. 当 accept 接到新连接，放入任务队列
3. 线程池中的空闲线程从队列取任务并处理
4. 处理完后关闭连接

## 要理解的关键问题

- 为什么单线程不够？（用 `telnet` 开两个连接验证）
- 线程池大小怎么定？
- 任务队列需要互斥锁保护吗？
- 一个线程在处理请求时阻塞了（如读大文件），其他线程还能接受新连接吗？
