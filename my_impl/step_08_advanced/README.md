# 里程碑 08：进阶功能

> **目标**：探索更深入的功能，根据兴趣选择方向。

## 可选方向

- **路由系统**：类 CivetWeb 的 URI 匹配模式
- **CGI 支持**：通过管道调用外部程序处理请求
- **Lua 脚本**：嵌入 Lua 解释器执行服务端脚本
- **性能优化**：epoll/kqueue 替代 select
- **REST API**：设计一套更友好的 REST 路由和 JSON 支持
- **WebDAV**：文件管理协议
- **断点续传**：Range 请求头支持
